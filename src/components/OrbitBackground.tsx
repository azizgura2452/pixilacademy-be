import React from 'react';
import '../styles/Orbit.css'; // Updated import path

const OrbitBackground: React.FC = () => {
  return (
    <div className="orbit-background">
      <div className="orbit-container">
        {/* Empty orbit; avatars are handled in the hero section */}
      </div>
    </div>
  );
};

export default OrbitBackground;
